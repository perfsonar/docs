******************************************
Importing Dashboards into Existing Grafana
******************************************

You can use existing Grafana instance to visualize your measurements data from perfSONAR archive. You can either :ref:`import perfSONAR dashboard templates <grafana_importing_templates>` into such Grafana and use them to extend your visualizations or :ref:`tell local pSConfig agent to set-up additional dashboards <grafana_importing_psconfig>` and data sources in a remote existing Grafana server.

    .. _grafana_importing_templates:
	
Importing Dashboard Templates into Existing Grafana
===================================================

You can import preconfigured perfSONAR dashboards into your own Grafana instance from perfSONAR repository. perfSONAR provides a few default dashboards with visualizations of your data:

* perfSONAR Endpoint Pair Explorer - enables browsing perfSONAR test results between selected source and destination measurement hosts. The metrics include: throughput, latency, packet loss, jiter, rtt, traceroute hop count and unique results. Test results are presented in both directions.
* perfSONAR Host Metrics - presents overview of selected measurement host status. This includes the history of perfSONAR services status, basic host hardware and software information and settings, CPU, RAM and disk utilization, pScheduler and pSConfig metrics.

.. note:: Importing perfSONAR dashboards into existing Grafana may require additional dashboard's confguration to tailor it to your local environment.

Download perfSONAR Dashboard
----------------------------

#. Open the following perfSONAR repository in your web browser:

    https://github.com/perfsonar/grafana/tree/master/perfsonar-grafana/perfsonar-grafana/dashboards/toolkit

#. Within the browser, click the selected JSON file.

#. On the selected JSON file view page, right-click the **Raw** button in the upper right-hand corner and  click **Save Link as...** which should allow you to save the .json file on your local machine.

Import a Dashboard
------------------

To import a dashboard, follow these steps:

#. Login as priviledged user to your Grafana instance.

#. Click **Dashboards** in the menu.

#. Click **New** and select **Import** in the drop-down menu.

#. Perform one of the following steps:

    * Upload a previously downloaded dashboard JSON file.
    * Paste dashboard JSON text directly into the JSON model text area.

#. Change UID, if required.

#. Click **Load**.

    .. _grafana_importing_psconfig:
	
Setting-up perfSONAR Dashboards in Existing Remote Grafana
===========================================================

You can point the `psconfig-grafana-agent` at an existing Grafana instance to add/modify dashboards on another remote Grafana instance. The pSConfig Grafana agent will connect to remote host and modify its Grafana instance with perfSONAR dashboards, similarly to the local instance.

In the Remote Grafana Server
----------------------------

#. Log in to the remote Grafana as a user with admin rights and create a Grafana service account following Grafana documentation https://grafana.com/docs/grafana/latest/administration/service-accounts/. Assign *Admin* role to your new service account.
#. Add a token to this service account.
#. Copy the generated token.

.. note:: For more information about creating service accounts contact the remote Grafana instance administrator.

In the Local Archive and pSConfig Server
----------------------------------------

#. Use the previously created token and set it in your local pSConfig so that it can modify remote Grafana desahboards::

    # psconfig agentctl grafana grafana-token <service-account-token>

#. Point the local `psconfig-grafana-agent` at a remote Grafana instance where dashboards and datasources will be built based on the local pSConfig::

    # psconfig agentctl grafana grafana-url <remote-grafana-url>